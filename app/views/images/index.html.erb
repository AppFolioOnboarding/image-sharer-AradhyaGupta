<h1> Images </h1>
<% stylesheet_link_tag "images.css" %>
<ul>
  <% @images.each do |image| %>
    <li>
      <%= image_tag(image.url, class: 'index-page_image') %>
      <ul>
        <%= render partial: 'images/tag_list', locals: {image: image} %>
      </ul>

    </li>
    <br><br>
  <% end %>
</ul>
